# 🦞 休息提醒助手 - 下载与安装指南

## 📦 文件位置

所有安装包都在服务器的以下目录：

```
/root/.openclaw/agents/dev/workspace/rest-reminder-gui/dist/
```

### 可用文件

| 文件名 | 大小 | 适用系统 | 说明 |
|--------|------|----------|------|
| 休息提醒助手-GUI.exe | 51 MB | Windows | ⭐推荐 - 图形界面版 |
| 休息提醒助手.exe | 47 MB | Windows | 命令行版 |
| 休息提醒助手 -1.0.0.AppImage | 100 MB | Linux | Linux 图形界面版 |
| rest-reminder-gui_1.0.0_amd64.snap | 84 MB | Linux | Snap 包 |

---

## 📥 下载方法

### 方法 1：直接复制文件（推荐）

如果你有服务器文件访问权限，直接复制文件到你的电脑：

**Windows 用户：**
```powershell
# 从服务器复制这两个文件之一到你的电脑
\\10.3.0.3\共享目录\rest-reminder-gui\dist\休息提醒助手-GUI.exe
```

**或者使用 SCP：**
```bash
# 从服务器下载
scp root@10.3.0.3:/root/.openclaw/agents/dev/workspace/rest-reminder-gui/dist/休息提醒助手-GUI.exe ./
```

### 方法 2：通过 HTTP 下载

下载服务器地址：`http://10.3.0.3:8080/`

**使用浏览器访问：**
```
http://10.3.0.3:8080/
```

**使用命令行下载：**

Windows (PowerShell):
```powershell
# 下载 GUI 版
Invoke-WebRequest -Uri "http://10.3.0.3:8080/休息提醒助手-GUI.exe" -OutFile "休息提醒助手-GUI.exe"

# 下载命令行版
Invoke-WebRequest -Uri "http://10.3.0.3:8080/休息提醒助手.exe" -OutFile "休息提醒助手.exe"
```

Linux:
```bash
# 下载 AppImage
wget http://10.3.0.3:8080/休息提醒助手 -1.0.0.AppImage
chmod +x 休息提醒助手 -1.0.0.AppImage
```

### 方法 3：使用 curl 下载

```bash
# Windows GUI 版
curl -O http://10.3.0.3:8080/休息提醒助手-GUI.exe

# Windows 命令行版
curl -O http://10.3.0.3:8080/休息提醒助手.exe

# Linux AppImage
curl -O http://10.3.0.3:8080/休息提醒助手 -1.0.0.AppImage
```

---

## 🔧 无法访问的解决方案

### 问题 1：连接超时

**原因：** 防火墙阻止 8080 端口

**解决：**
```bash
# 临时开放 8080 端口（需要 root 权限）
sudo iptables -I INPUT -p tcp --dport 8080 -j ACCEPT

# 或者使用 ufw
sudo ufw allow 8080/tcp
```

### 问题 2：IP 地址错误

**检查当前 IP：**
```bash
hostname -I
```

### 问题 3：服务器未启动

**重启下载服务器：**
```bash
cd /root/.openclaw/agents/dev/workspace/rest-reminder-gui
node server.js
```

---

## 🚀 使用方法

### Windows GUI 版

1. 双击 `休息提醒助手-GUI.exe`
2. 点击「开始工作」按钮
3. 45 分钟后自动提醒休息
4. 关闭窗口会最小化到系统托盘

### Windows 命令行版

1. 双击 `休息提醒助手.exe`
2. 按 `1` 开始工作
3. 按 `2` 打开设置
4. 按 `3` 退出程序

### Linux AppImage 版

```bash
# 添加执行权限
chmod +x 休息提醒助手 -1.0.0.AppImage

# 运行
./休息提醒助手 -1.0.0.AppImage
```

---

## 📸 功能截图

### GUI 版界面
- 紫色渐变主题
- 大号计时器显示
- 实时进度条
- 使用统计面板
- 设置面板

### 命令行版界面
- 简单文字界面
- 进度条显示
- 配置菜单

---

## ⚙️ 配置说明

首次运行会自动创建 `config.json`：

```json
{
  "workDuration": 45,      // 工作时长（分钟）
  "restDuration": 5,       // 休息时长（分钟）
  "enableLock": false,     // 休息时锁屏
  "sound": true            // 提示音
}
```

---

## 🎯 推荐

**Windows 用户：** 强烈推荐使用 **GUI 版**（休息提醒助手-GUI.exe）

**理由：**
- ✅ 漂亮的图形界面
- ✅ 系统托盘支持
- ✅ 使用统计功能
- ✅ 一键操作
- ✅ 防止系统休眠

---

## 📞 技术支持

**作者：** 龙虾编辑部 - 技术开发  
**许可证：** MIT

如有问题，请检查：
1. 文件是否完整下载
2. Windows 版本是否兼容（Win10+）
3. 杀毒软件是否误报（添加信任即可）

---

💡 **健康提示：** 长时间工作有害健康，记得定时休息！
